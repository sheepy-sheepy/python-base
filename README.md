# Задания для `lesson1.py`

1. Дана строка: `work = "ha"`. Выведи смех из `7` повторений подряд.  
   Ожидаемый результат: `hahahahahahaha`

2. Даны числа `a = 17`, `b = 4`. Выведи по отдельности: `a / b`, `a // b`,
   `a % b`, `a ** b`

3. Даны строки `x = "2026"` и `y = "17.77"`. Преобразуй: `x` в `int`, `x` в
   `float`, `y` в `float`.  
   Затем выведи типы всех полученных значений через `type`.

4. Исправь код ниже так, чтобы он работал и печатал:  
   `Total: 150`

   ```python
   total = 100
   bonus = "50"
   print("Total: " + total + bonus)

5. Дана строка: `price = "12a"`. Попробуй преобразовать её в `int` и посмотри
   на ошибку. В качестве решения напиши, какая ошибка возникает и почему.

6. Напиши 6 строк кода, которые печатают результат `bool(...)` для значений:
   `0, 1, 0.0, "", "0", None`. В комментарии к решению опиши полученный
   результат.

# Задание для `lesson2.py`

1. Создайте две переменных `is_sunny` и `is_weekend` типа `bool`.

Напишите программу, которая будет выводить следующий результат:

- ЕСЛИ  
  `is_sunny` И `is_weekend`, вывести "Идеальный день для прогулки"

- ЕСЛИ  
  `is_sunny` НО НЕ `is_weekend`, вывести "Погода хорошая, но нужно поработать"

- ЕСЛИ  
  НЕ `is_sunny` НО `is_weekend`, вывести "Можно остаться дома и отдохнуть"

- ЕСЛИ  
  НЕ `is_sunny` И НЕ `is_weekend`, вывести "Рабочий день с плохой погодой"

# Задание для `lesson3.py`

1. Выведи в терминал числа `3`, `6`, `9`, используя цикл `for` и функцию
   `range`

2. Дано число `n` (вводится с клавиатуры). Посчитай сумму чисел от `1` до `n`
   включительно, и выведи результат в терминал

3. Дополни предыдущее задание тем, чтобы суммировались только чётные числа

4. Пользователь вводит числа, пока не введёт `0`. Выведи сумму всех введённых.
   Используй цикл `while`

5. Пользователь вводит числа `10` раз. Если встретилось отрицательное число —
   нужно сразу прервать цикл. Выведи сумму всех введённых до отрицательного

6. Выведи числа от `1` до `30`, пропуская те, которые делятся на `3` без
   остатка. Используй `continue`. Пример начала вывода: `1 2 4 5 7 8 10 11 ...`

# Задание для `lesson4.py`

1. Вводится строка `message`.
    - Убери все пробелы по краям
    - Если после этого строка пустая — выведи `EMPTY`
    - Иначе если в сообщении встречается `"spam"` в любом регистре — выведи
      `BLOCKED`

2. Вводится строка `s`, число `i`.
    - Если `i` в пределах строки — выведи символ `s[i]`
    - А также `True/False`, является ли он буквой `"a"` / `"A"` (регистр не
      важен)
    - Если `i` не в пределах строки — выведи `IndexError`
    - Подсказка: для проверки используй `len`

3. Вводится строка `full_name` (например: `"aLexaNdEr the gReAt"`).
    - Убери пробелы по краям
    - Приведи к нормальному виду: первая буква каждого слова заглавная,
      остальные строчные
    - Выведи: первый символ строки и последний символ строки через точку –
      `A.t`

# Задание для `lesson5.py`

1. Дан список: `numbers = [12, 5, 8, 99, 3, 8]`
    1) Выведи первый и последний элемент
    2) Проверь, есть ли в списке число `8`
    3) Выведи сумму, минимальный и максимальный элемент

2. Дан список: `gods = ['Zeus', 'Apollo', 'Ares']`
    1) Добавь в конец списка `Hermes`
    2) Вставь `Hades` после `Zeus`
    3) Удали элемент `Apollo`
    4) Удали последний элемент через `pop()`, сохранив его в переменную
    5) Выведи итоговый список и удалённый элемент

3. Дан список: `numbers = [4, 7, 10, 15, 18, 21, 24]`
    1) Выведи все элементы списка по одному в цикле
    2) Выведи только чётные числа
    3) С помощью `enumerate` выведи индекс и значение каждого элемента
    4) Выведи, сколько в списке чисел больше `10`

4. Пользователь вводит текст: `text = input()`
    1) Разбей его на слова и сохрани в список
    2) Склей список обратно в строку с разделителем `-`

Пример ввода:
`There is nothing impossible to him who will try`

Пример вывода:

```
>>> ['There', 'is', 'nothing', 'impossible', 'to', 'him', 'who', 'will', 'try']
>>> There-is-nothing-impossible-to-him-who-will-try
```

5. Создай список квадратов чисел от `1` до `10` с помощью списочного выражения,
   исключая числа, которые делятся на `3`

Ожидаемый результат:
`[1, 4, 16, 25, 49, 64, 100]`

# Задание для `lesson6.py`

1. Создай словарь `user` с любой информацией о себе
    - Выведи имя
    - Выведи фамилию через `get`
    - Измени город на новый
    - Добавь новый ключ `learning_python` со значением `True / False`
    - Удали ключ города
    - Через цикл `for` выведи каждое ключ-значение в формате:
      `"Ключ – значение"`

# Задание для `lesson7.py`

1. Создай кортеж `student`, в котором хранятся: имя, возраст, язык
   программирования.  
   Распакуй кортеж в переменные `name`, `age` и `language` и выведи в терминал.

2. Есть список слов:
   `words = ["python", "code", "list", "python", "set", "code", "tuple"]`
    1) Выведи количество уникальных слов в нём (используй множество)
    2) Проверь, есть ли в нём слово `"tuple"`
    3) Добавь слово `"loop"`
    4) Удали слово `"list"`

# Задание для `lesson8.py`

1. Напиши функцию `print_ticket(name, film)`, которая выводит строку:
   `Билет для <name> на фильм <film>`. Вызови её для трёх разных людей

2. Напиши функцию `get_square(number)`, которая возвращает квадрат числа.
   Сохрани результат в переменную, прибавь `10` и выведи на экран

Пример:

```python
result = get_square(4)
print(result + 10)  # 26
```

3. Напиши функцию `check_age(age)`, которая возвращает:

- 'Ребёнок', если возраст меньше `12`
- 'Подросток', если возраст от `12` до `17`
- 'Взрослый', если возраст `18` и больше

Пример:

```python
print(check_age(15))  # Подросток
```

4. Что выведет этот код?

```python
city = 'Moscow'


def show_city():
    city = 'London'
    print(city)


show_city()
print(city)
# 1. Предскажи вывод
# 2. Запусти код и проверь результат
# 3. Объясни, почему вывод именно такой
```

5. Напиши функцию `get_average(*args)`, которая принимает любое количество
   чисел и возвращает их среднее арифметическое.

Пример:

```python
print(get_average(1, 2, 3, 4))  # 2.5
```

# Задание для `lesson9.py`

1. Есть словарь: `user = {'name': 'Kiril', 'age': 18}`

Попробуй получить ключ `'email'`. Обработай появившуюся ошибку так, чтобы
вместо неё было выброшено исключение `ValueError` с текстом
`'Ключа email нет'`.

# `Тема 10` - импорты и библиотеки (import, venv)

# Задание для `lesson11.py`

1. Создай класс `GameCharacter`

В `__init__` передаются: `name`, `damage`, `health` и сохраняются в атрибуты
экземпляра

Добавь методы:

-  `attack` — должен выводить строку `<name> наносит <damage> урона`
-  `take_damage(amount)` — Уменьшает `health` на `amount`. Если `health` стало
меньше нуля, сделай `health = 0`.
Выводит строку — `<name> получил <amount> урона. Осталось здоровья: <health>`
-  Создай персонажа. Вызови `attack`. Несколько раз вызови `take_damage`.
Выведи итоговое значение `health`

2. Создай класс `Cart`

В `__init__` передается `owner` — имя владельца корзины. А также создается
пустой список `self.items` (список словарей)  
Каждый товар в виде: `{"name": "Phone", "price": 50000}`

Добавь методы:

-  `add_item(name, price)` — добавляет товар в `self.items`
-  `get_total()` — Возвращает общую стоимость всех товаров в корзине
-  `show_items()` — Выводит товары в формате:

```
Phone – 50000
Mouse – 1500
```
Если товаров нет — выводит `Корзина пуста`

# Контекстный менеджер в `lesson12.py`

# Декоратор в `lesson13.py`
